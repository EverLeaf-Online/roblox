return table.freeze({
    staggered = table.freeze({
        DisplayName = "Staggered",
        ShortName = "STAGGER",
        Kind = "Debuff",
        Description = "Briefly disrupted by an incoming hit.",
    }),
})
