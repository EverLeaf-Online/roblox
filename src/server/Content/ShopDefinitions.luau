return table.freeze({
    lumen_supply = table.freeze({
        DisplayName = "Lumen Supply",
        Currency = "Shards",
        Items = table.freeze({ small_tonic = 2, training_blade = 8 }),
    }),
})
